-- Drop database schema if exists
-- DROP SCHEMA IF EXISTS let_it_fly;

-- Create a new database schema
CREATE SCHEMA IF NOT EXISTS let_it_fly;

-- Drop exist user
-- DROP USER IF EXISTS 'letitfly'@'localhost';

-- Create new user
-- CREATE USER 'letitfly'@'localhost' IDENTIFIED BY 'letitfly';

-- Grant privileges for this user to new schema
-- GRANT ALL PRIVILEGES ON let_it_fly.* TO 'letitfly'@'localhost';